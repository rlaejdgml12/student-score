# 2026-08-08 학습 노트 — 3주차: 자바 중급1 (Object~날짜와 시간)

## 01 Object 클래스
- equals 재정의 규칙 (대칭성 등):
- instanceof vs getClass (상속 관계에서의 차이):
- equals-hashCode가 짝으로 움직여야 하는 이유:

## 02 불변 객체
- 불변 객체가 필요한 이유 (사이드 이펙트):
- 방어적 복사란:
- List.copyOf()가 하는 두 가지 일:

## 03 String 클래스
- String Pool과 리터럴:
- new String()과의 차이:
- intern()이 하는 일:
- replace() 등이 원본을 안 바꾸는 이유:
- StringBuilder를 쓰는 이유:

## 04 래퍼 클래스
- 기본형과 래퍼 클래스의 차이:
- Integer 캐시 범위와 == 함정:

## 05 열거형 Enum
- Enum을 if-else 대신 쓰는 이유:
- 상수별 메서드 구현이란:
- values()가 하는 일:

## 06 날짜와 시간
- LocalDateTime과 ZonedDateTime의 차이:
- Period.getDays()의 함정:
- 총 경과 일수를 구할 때 써야 하는 것:
- DST(서머타임)와 plusHours vs plusDays 차이:

## 오늘 만든 것 (StudentScore)
- v6 (equals/hashCode):
- v7 (불변 객체, subjects 방어적 복사):
- v8 (Grade Enum, 패키지 분리):

## 트러블슈팅
- 겪은 문제:
- 원인:
- 해결:

## 질문한 것 / 헷갈렸던 것
-
-
